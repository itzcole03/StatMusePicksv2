"""
Top-level tests package marker.

This file makes the `tests` directory importable as a package which
resolves test collection import issues in some environments. Keeping this
empty and minimal avoids changing test behavior.
"""
"""Test package marker for top-level tests to allow package import semantics during pytest collection."""
"""Package marker for top-level tests to enable package-style imports during pytest discovery."""
