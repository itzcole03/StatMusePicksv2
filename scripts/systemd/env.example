# Example environment file for the StatMusePicksV2 systemd service
# Copy to: /etc/statmuse/env  (use secure permissions, e.g. chmod 600)
# Replace values with your environment-specific paths and secrets

# Absolute path to the repository root where `scripts/run_daily_sync.sh` lives
REPO_ROOT=/srv/statmusepicks

# Optional: audit directory where raw fetches are stored
INGEST_AUDIT_DIR=/var/lib/statmuse/ingest_audit

# Optional webhook for ingest alerts (example: internal alert receiver)
INGEST_ALERT_WEBHOOK=https://hooks.example.com/ingest-alerts

# Optional HMAC secret for signing webhook payloads (keep secret)
# Use a secure random value and rotate periodically
INGEST_ALERT_HMAC_SECRET=replace_with_a_strong_random_value

# Optional: run the job as a non-root user name (matches systemd User=)
SERVICE_USER=statmuse
